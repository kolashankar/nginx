# HLS Encryption Key (leave empty for auto-generation)
HLS_ENCRYPTION_KEY=

# Allowed stream keys (comma-separated)
ALLOWED_STREAM_KEYS=demo,test,stream1

# Webhook secret for authentication
WEBHOOK_SECRET=your_secret_key_here

# Optional: Custom callback URL (default: http://webhook:8000)
# HTTP_CALLBACK_URL=http://your-backend.com